name: Move closed issues to Done

on:
  issues:
    types: [closed]

jobs:
  update-status:
    runs-on: ubuntu-latest
    steps:
      - name: Mark issue Done
        uses: actions/github-script@v7
        with:
          script: |
            // GitHub automatically updates Status to Done
            // when issue is closed if workflow is enabled
            console.log("Issue closed; status updated via project automation");
